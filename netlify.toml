[build]
  base = "taskflow"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_ENV = "production"
  NEXTAUTH_URL = "https://your-site.netlify.app"

[[plugins]]
  package = "@netlify/plugin-nextjs"
